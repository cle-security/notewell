{
  "$defs": {
    "AgentConfig": {
      "additionalProperties": false,
      "description": "LLM and concurrency knobs for the verifier.",
      "properties": {
        "model": {
          "default": "anthropic:claude-sonnet-4-6",
          "description": "Pydantic-AI model identifier.",
          "examples": [
            "anthropic:claude-sonnet-4-6",
            "openai:gpt-4o",
            "google-gla:gemini-2.0-flash"
          ],
          "title": "Model",
          "type": "string",
          "markdownDescription": "Pydantic-AI model identifier."
        },
        "max_concurrency": {
          "default": 4,
          "minimum": 1,
          "title": "Max Concurrency",
          "type": "integer"
        },
        "timeout_seconds": {
          "default": 300,
          "minimum": 1,
          "title": "Timeout Seconds",
          "type": "integer"
        },
        "thinking": {
          "$ref": "#/$defs/ThinkingLevel",
          "default": true,
          "description": "Enable provider extended-thinking / reasoning for verifier runs. `true` uses the provider's default; a string picks an explicit effort level; `false` disables.",
          "markdownDescription": "Enable provider extended-thinking / reasoning for verifier runs. `true` uses the provider's default; a string picks an explicit effort level; `false` disables."
        }
      },
      "title": "AgentConfig",
      "type": "object",
      "markdownDescription": "LLM and concurrency knobs for the verifier."
    },
    "ObservabilityConfig": {
      "additionalProperties": false,
      "description": "Project-level Logfire / tracing preferences.\n\nTracing is opt-in via the `LOGFIRE_TOKEN` env var; this section\nonly controls how spans are produced once tracing is on.",
      "properties": {
        "dangerously_disable_log_scrubbing": {
          "default": false,
          "description": "Disable Logfire's default secret-pattern scrubber (which redacts attribute values matching `password`, `token`, `secret`, etc.). For a security-review tool the scrubber is mostly false positives \u2014 the agent legitimately reads code containing those words. Set true only when you trust the dashboard the `LOGFIRE_TOKEN` points at; raw span content (including any real secrets the codebase exposes) will reach it.",
          "title": "Dangerously Disable Log Scrubbing",
          "type": "boolean",
          "markdownDescription": "Disable Logfire's default secret-pattern scrubber (which redacts attribute values matching `password`, `token`, `secret`, etc.). For a security-review tool the scrubber is mostly false positives \u2014 the agent legitimately reads code containing those words. Set true only when you trust the dashboard the `LOGFIRE_TOKEN` points at; raw span content (including any real secrets the codebase exposes) will reach it."
        }
      },
      "title": "ObservabilityConfig",
      "type": "object",
      "markdownDescription": "Project-level Logfire / tracing preferences.\n\nTracing is opt-in via the `LOGFIRE_TOKEN` env var; this section\nonly controls how spans are produced once tracing is on."
    },
    "ReporterConfig": {
      "additionalProperties": false,
      "description": "One reporter instance. `output` is required for file-emitting reporters.",
      "properties": {
        "type": {
          "$ref": "#/$defs/ReporterName"
        },
        "output": {
          "anyOf": [
            {
              "format": "path",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Output"
        }
      },
      "required": [
        "type"
      ],
      "title": "ReporterConfig",
      "type": "object",
      "markdownDescription": "One reporter instance. `output` is required for file-emitting reporters."
    },
    "ReporterName": {
      "enum": [
        "pretty",
        "sarif",
        "sarif-github",
        "json",
        "markdown",
        "github"
      ],
      "type": "string"
    },
    "RequirementExclusion": {
      "additionalProperties": false,
      "description": "A user-authored declaration that a requirement does not apply.\n\nEach exclusion carries a human-readable rationale, mirroring the lock\nfile's agent-discovered exclusions so a reviewer can see why any given\nrequirement was skipped.",
      "properties": {
        "requirement": {
          "description": "Requirement id or fnmatch glob to drop.",
          "examples": [
            "V8.3.1",
            "V8.3.*"
          ],
          "minLength": 1,
          "title": "Requirement",
          "type": "string",
          "markdownDescription": "Requirement id or fnmatch glob to drop."
        },
        "reason": {
          "description": "Human-readable rationale; surfaced in logs and reports.",
          "minLength": 1,
          "title": "Reason",
          "type": "string",
          "markdownDescription": "Human-readable rationale; surfaced in logs and reports."
        }
      },
      "required": [
        "requirement",
        "reason"
      ],
      "title": "RequirementExclusion",
      "type": "object",
      "markdownDescription": "A user-authored declaration that a requirement does not apply.\n\nEach exclusion carries a human-readable rationale, mirroring the lock\nfile's agent-discovered exclusions so a reviewer can see why any given\nrequirement was skipped."
    },
    "RequirementSelector": {
      "additionalProperties": false,
      "description": "Filters applied on top of the requirements pulled from the baseline.",
      "properties": {
        "include": {
          "description": "Requirement ids or glob patterns to keep.",
          "examples": [
            [
              "V5.1.3",
              "V5.*"
            ]
          ],
          "items": {
            "type": "string"
          },
          "title": "Include",
          "type": "array",
          "markdownDescription": "Requirement ids or glob patterns to keep."
        },
        "exclude": {
          "description": "Requirements to drop, each with a rationale.",
          "examples": [
            [
              {
                "reason": "No file uploads in this codebase.",
                "requirement": "V8.3.*"
              }
            ]
          ],
          "items": {
            "$ref": "#/$defs/RequirementExclusion"
          },
          "title": "Exclude",
          "type": "array",
          "markdownDescription": "Requirements to drop, each with a rationale."
        }
      },
      "title": "RequirementSelector",
      "type": "object",
      "markdownDescription": "Filters applied on top of the requirements pulled from the baseline."
    },
    "ScopeConfig": {
      "additionalProperties": false,
      "description": "Which files in the repo are eligible for analysis.\n\nScope filtering is for **files the verifier shouldn't bother\nanalyzing** \u2014 vendored dependencies, build outputs, tests with\nintentional security flaws. It is NOT a place to limit what the\nverifier *checks*; for that, use `requirements.exclude`, which\ncarries an explicit rationale per excluded requirement.\n\nSensible defaults are applied automatically (gitignore, common\nbuild and cache directories, binary file extensions). Most\nprojects don't need to set anything here at all.",
      "properties": {
        "include": {
          "description": "Glob patterns to include. Empty (the default) means the whole repo, minus the automatic skips and any patterns in `exclude`. Use this only when you genuinely need to restrict analysis to a subset (e.g. monorepo scoping) \u2014 NOT to limit which requirements run. README, package.json, Dockerfile, and similar config/manifest files are often load-bearing for verification; restricting to source directories will hide them from the verifier.",
          "items": {
            "type": "string"
          },
          "title": "Include",
          "type": "array",
          "markdownDescription": "Glob patterns to include. Empty (the default) means the whole repo, minus the automatic skips and any patterns in `exclude`. Use this only when you genuinely need to restrict analysis to a subset (e.g. monorepo scoping) \u2014 NOT to limit which requirements run. README, package.json, Dockerfile, and similar config/manifest files are often load-bearing for verification; restricting to source directories will hide them from the verifier."
        },
        "exclude": {
          "description": "Glob patterns to exclude, applied after gitignore and the built-in skips. Common additions: `**/*.test.ts`, `**/*.spec.ts`, `**/__tests__/**`, `**/fixtures/**`, `**/*.min.js`.",
          "items": {
            "type": "string"
          },
          "title": "Exclude",
          "type": "array",
          "markdownDescription": "Glob patterns to exclude, applied after gitignore and the built-in skips. Common additions: `**/*.test.ts`, `**/*.spec.ts`, `**/__tests__/**`, `**/fixtures/**`, `**/*.min.js`."
        },
        "respect_gitignore": {
          "default": true,
          "title": "Respect Gitignore",
          "type": "boolean"
        }
      },
      "title": "ScopeConfig",
      "type": "object",
      "markdownDescription": "Which files in the repo are eligible for analysis.\n\nScope filtering is for **files the verifier shouldn't bother\nanalyzing** \u2014 vendored dependencies, build outputs, tests with\nintentional security flaws. It is NOT a place to limit what the\nverifier *checks*; for that, use `requirements.exclude`, which\ncarries an explicit rationale per excluded requirement.\n\nSensible defaults are applied automatically (gitignore, common\nbuild and cache directories, binary file extensions). Most\nprojects don't need to set anything here at all."
    },
    "Suppression": {
      "additionalProperties": false,
      "description": "A reviewed, time-bounded acceptance of a known finding.\n\nScope is set by either `path` (location-based, glob-aware) or\n`fingerprint` (content-based, location-independent) \u2014 they are\nmutually exclusive. With neither, the suppression applies repo-wide\nto every finding raised by `requirement`.",
      "properties": {
        "requirement": {
          "description": "Requirement ID this suppresses.",
          "minLength": 1,
          "title": "Requirement",
          "type": "string",
          "markdownDescription": "Requirement ID this suppresses."
        },
        "path": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Path or glob the suppression applies to. Omit for repo-wide.",
          "title": "Path",
          "markdownDescription": "Path or glob the suppression applies to. Omit for repo-wide."
        },
        "fingerprint": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Stable finding fingerprint, for code-location-independent suppression.",
          "title": "Fingerprint",
          "markdownDescription": "Stable finding fingerprint, for code-location-independent suppression."
        },
        "reason": {
          "description": "Human explanation; required so the rationale is auditable.",
          "minLength": 1,
          "title": "Reason",
          "type": "string",
          "markdownDescription": "Human explanation; required so the rationale is auditable."
        },
        "expires": {
          "anyOf": [
            {
              "format": "date",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Date after which the suppression no longer applies.",
          "title": "Expires",
          "markdownDescription": "Date after which the suppression no longer applies."
        }
      },
      "required": [
        "requirement",
        "reason"
      ],
      "title": "Suppression",
      "type": "object",
      "markdownDescription": "A reviewed, time-bounded acceptance of a known finding.\n\nScope is set by either `path` (location-based, glob-aware) or\n`fingerprint` (content-based, location-independent) \u2014 they are\nmutually exclusive. With neither, the suppression applies repo-wide\nto every finding raised by `requirement`."
    },
    "ThinkingLevel": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "enum": [
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh"
          ],
          "type": "string"
        }
      ]
    }
  },
  "additionalProperties": false,
  "description": "Top-level config object loaded from `cle.yaml`.",
  "properties": {
    "version": {
      "const": 1,
      "default": 1,
      "description": "Config schema version. Bumped on breaking changes.",
      "title": "Version",
      "type": "integer",
      "markdownDescription": "Config schema version. Bumped on breaking changes."
    },
    "baseline": {
      "anyOf": [
        {
          "const": "asvs-l1",
          "title": "asvs-l1",
          "description": "The Application Security Verification Standard is a list of application security requirements that architects, developers, testers, security professionals, tool vendors, and consumers can use to define, build, test, and verify secure applications.\n\nLevel 1: critical starting point covering ~20% of requirements. Focuses on first-layer defenses against common attacks that don't require other vulnerabilities to be exploitable.",
          "markdownDescription": "The Application Security Verification Standard is a list of application security requirements that architects, developers, testers, security professionals, tool vendors, and consumers can use to define, build, test, and verify secure applications.\n\nLevel 1: critical starting point covering ~20% of requirements. Focuses on first-layer defenses against common attacks that don't require other vulnerabilities to be exploitable."
        },
        {
          "const": "asvs-l2",
          "title": "asvs-l2",
          "description": "The Application Security Verification Standard is a list of application security requirements that architects, developers, testers, security professionals, tool vendors, and consumers can use to define, build, test, and verify secure applications.\n\nLevel 2: recommended for most applications, covering ~70% of requirements. Adds protections against less common attacks and more complex defenses that may require certain preconditions to exploit.",
          "markdownDescription": "The Application Security Verification Standard is a list of application security requirements that architects, developers, testers, security professionals, tool vendors, and consumers can use to define, build, test, and verify secure applications.\n\nLevel 2: recommended for most applications, covering ~70% of requirements. Adds protections against less common attacks and more complex defenses that may require certain preconditions to exploit."
        },
        {
          "const": "asvs-l3",
          "title": "asvs-l3",
          "description": "The Application Security Verification Standard is a list of application security requirements that architects, developers, testers, security professionals, tool vendors, and consumers can use to define, build, test, and verify secure applications.\n\nLevel 3: highest level of security covering all requirements. Adds defense-in-depth mechanisms and hard-to-implement controls for applications demanding the strongest assurance.",
          "markdownDescription": "The Application Security Verification Standard is a list of application security requirements that architects, developers, testers, security professionals, tool vendors, and consumers can use to define, build, test, and verify secure applications.\n\nLevel 3: highest level of security covering all requirements. Adds defense-in-depth mechanisms and hard-to-implement controls for applications demanding the strongest assurance."
        },
        {
          "const": "masvs",
          "title": "masvs",
          "description": "The OWASP MASVS (Mobile Application Security Verification Standard) is the industry standard for mobile app security. It can be used by mobile software architects and developers seeking to develop secure mobile apps, as well as security testers to ensure completeness and consistency of test results.",
          "markdownDescription": "The OWASP MASVS (Mobile Application Security Verification Standard) is the industry standard for mobile app security. It can be used by mobile software architects and developers seeking to develop secure mobile apps, as well as security testers to ensure completeness and consistency of test results."
        },
        {
          "type": "string",
          "description": "Path to a custom baseline YAML file.",
          "markdownDescription": "Path to a custom baseline YAML file."
        }
      ],
      "description": "Built-in baseline identifier or path to a custom baseline file.",
      "default": "asvs-l2",
      "markdownDescription": "Built-in baseline identifier or path to a custom baseline file."
    },
    "requirements": {
      "$ref": "#/$defs/RequirementSelector"
    },
    "scope": {
      "$ref": "#/$defs/ScopeConfig"
    },
    "suppressions": {
      "items": {
        "$ref": "#/$defs/Suppression"
      },
      "title": "Suppressions",
      "type": "array"
    },
    "agent": {
      "$ref": "#/$defs/AgentConfig"
    },
    "default_reporter": {
      "$ref": "#/$defs/ReporterName",
      "default": "pretty",
      "description": "Reporter used when `--reporter` is not passed on the command line and no `reporters` are configured. Individual reporter outputs are configured via `reporters`.",
      "markdownDescription": "Reporter used when `--reporter` is not passed on the command line and no `reporters` are configured. Individual reporter outputs are configured via `reporters`."
    },
    "reporters": {
      "items": {
        "$ref": "#/$defs/ReporterConfig"
      },
      "title": "Reporters",
      "type": "array"
    },
    "observability": {
      "$ref": "#/$defs/ObservabilityConfig"
    }
  },
  "title": "Cl\u00e9 configuration",
  "type": "object",
  "markdownDescription": "Top-level config object loaded from `cle.yaml`."
}
